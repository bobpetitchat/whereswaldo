import React from "react"

const Leaderboard = () => {
  const buttonStyle = {
    outline: 0,
    cursor: "pointer",
    color: "#EE2923",
    backgroundColor: "#fff",
    fontWeight: 800,
    fontSize: "18px",
    height: "auto",
    width: "70px",
    border: "none"
  }
  
  return (
    <div style={buttonStyle}>Leaderboard</div>
  )
}

export default Leaderboard